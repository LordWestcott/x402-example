# x402 Client Configuration

# ============================================
# Wallet Configuration
# ============================================

# Option 1: CDP Server Wallet (Recommended)
# Get these credentials from https://cdp.coinbase.com/
# Set USE_CDP_WALLET=true to use this option
USE_CDP_WALLET=true
CDP_API_KEY_ID=your-api-key-id
CDP_API_KEY_SECRET=your-api-key-secret
CDP_WALLET_SECRET=your-wallet-secret

# Option 2: Private Key Wallet (Alternative)
# Set USE_CDP_WALLET=false and provide your private key
# USE_CDP_WALLET=false
# PRIVATE_KEY=your-private-key-here

# ============================================
# API Configuration
# ============================================

# URL of the protected endpoint
# Defaults to http://localhost:3000/locked if not set
API_URL=http://localhost:3000/locked

# ============================================
# Retry Configuration (Optional)
# ============================================

# Maximum number of retry attempts (default: 3)
# MAX_RETRIES=3

# Initial retry delay in milliseconds (default: 2000 = 2 seconds)
# RETRY_DELAY_MS=2000

# Maximum retry delay in milliseconds (default: 30000 = 30 seconds)
# MAX_RETRY_DELAY_MS=30000

